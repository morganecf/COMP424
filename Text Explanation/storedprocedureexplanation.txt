Our create procedure statement:

CREATE PROCEDURE Allocate_Patrol_Officer (IN threshold INT) 
PARAMETER STYLE JAVA
LANGUAGE JAVA
EXTERNAL NAME 'StoredProcedure.updateAllocation'

returned "cs421g10 does not have privilege to perform operation "CREATE PROCEDURE"

Our stored procedure is on the db2 server under StoredProcedure.java - executable is StoredProcedure.class (it has also been included in the project for your review)
However due to these issues we were not able to implement the storedprocedure fully